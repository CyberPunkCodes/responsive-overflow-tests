# CLAUDE.md

This project's agent instructions live in [AGENTS.md](AGENTS.md). Read that
file before making changes.

(A plain pointer rather than a symlink on purpose: Windows checkouts without
`core.symlinks` turn a committed symlink into a text file containing the path,
which silently breaks it.)
